<%- include('partials/header', { title: 'Notice', user, canViewStudents: true, canViewFees: true }) %>
  <div class="card card-pad" style="max-width: 560px; margin: 40px auto; text-align: center;">
    <div style="font-size: 32px; margin-bottom: 10px;">⚠️</div>
    <h1 style="font-size: 17px; margin: 0 0 8px;">Can't complete that</h1>
    <p style="color: var(--text-muted); margin: 0 0 20px;"><%= message %></p>
    <a href="/" class="btn btn-outline">Back to dashboard</a>
  </div>
<%- include('partials/footer') %>
